import React, { Component } from 'react';
import {Button} from 'antd';

class Demo extends Component {
    render() {
        return (
            <div>
               <Button type="primary">Hook 的案例</Button> 
            </div>
        );
    }
}

export default Demo;